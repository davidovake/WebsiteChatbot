import styled, { css } from "styled-components";

export const FooterRoot = styled.div(
  ({ theme }) => css`
    height: 80px;
    min-height: 80px;
    background-color: #4281f5;
    color: black;
    align-self: flex-end;
    width: 100%;
  `
);
